# 3. Expresar, utilizando operadores aritméticos, as seguintes expresións

m = 10.0
n = 5.0
p = 4.0
q = 2.0
r = 3.0
s = 6.0
c = 100.0
t = 2.0
i = 0.05

print ("a)", (m + n) / n)
print ("b)", ((m + n) / p) / ((p - r) * s))
print ("c)", (m + 4) / (p - q))
print ("d)", (c * r * t) / 100)
print ("e)", (m + n) / (p + (q / r)))
print ("f)", (m / n) * (p + q))
print ("g)", (n * (1 + t)**t * i) / ((1 + i)**t - 1))